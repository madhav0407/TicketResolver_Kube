apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose --file docker-compose.yml convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: ticketresolver-frontend
  name: ticketresolver-frontend-service
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  type:
    LoadBalancer
  selector:
    io.kompose.service: ticketresolver-frontend
